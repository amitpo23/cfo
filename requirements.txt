# Core Dependencies
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0.post1

# Security & Authentication
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.13.1
psycopg[binary]==3.2.9

# API & Web
fastapi==0.108.0
uvicorn[standard]==0.25.0
httpx==0.26.0

# Data Processing
pandas==2.1.4
numpy==1.26.2

# Optional ML forecasting packages are intentionally excluded from the default
# production install because Vercel Python Functions have a 500 MB bundle limit.
# Use requirements-full.txt for local development with TensorFlow/Prophet/XGBoost.

# Accounting Integrations
# quickbooks-online==0.8.0  # Uncomment when needed
# xero-python==3.0.0  # Uncomment when needed

# Reports & Visualization
openpyxl==3.1.2
reportlab==4.0.7
matplotlib==3.8.2

# CLI
click==8.1.7
rich==13.7.0

# AI & NLP (for financial insights)
openai==1.6.1

# Expense OCR pipeline (vision extraction of receipt scans).
# Anthropic is preferred — reads receipt PDFs natively (no rasterization).
# pypdfium2 + Pillow are only needed for the OpenAI fallback (rasterizes
# PDF -> image; Pillow backs pypdfium2's to_pil()).
anthropic==0.40.0
pypdfium2==4.30.0
Pillow==11.3.0

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
