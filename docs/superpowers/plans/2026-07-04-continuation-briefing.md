# תדרוך המשך — רצף (Rezef), אחרי 13 איטרציות של לולאת השיפור המתמדת

> **לסוכן המתדרך:** זהו מסמך תדרוך (briefing), לא תכנית ביצוע. המשימה שלך:
> לענות על כל השאלות הפתוחות בסעיף 3 (מחקר/החלטה מבוססת-קוד, לא ניחוש —
> לכל שאלה שדורשת קלט מהמשתמש בפועל, סמן זאת בבירור ואל תחליט לבד), ואז
> להפיק **תכנית המשך קונקרטית** (שלבים, קבצים, טסטים) ולשמור אותה כקובץ
> `docs/superpowers/plans/2026-07-04-continuation-plan.md`. עבוד מתוך
> `/Users/mymac/coding/cfo`, branch `feat/sumit-ar-ap-documents-ocr`.
> תיעוד ותקשורת בעברית. מקור-אמת ליומן ההתקדמות המלא:
> `.superpowers/sdd/progress.md` (65 קומיטים מאז תחילת גל 2 של הסשן הזה,
> 13 רשומות "CONTINUOUS-IMPROVEMENT LOOP — iteration N").

## 1. מטרת-העל (ללא שינוי)

`docs/superpowers/plans/2026-07-03-rezef-goal-directive.md` — הבאת רצף
למצב שמחליף באופן מלא מנהל כספים והנהלת חשבונות. 7 יעדי-סיום מדידים שם,
בסעיף "יעדי הסיום המדידים". **עצור רק לפי כללי-העצירה שבמסמך ההוא**
(פעולה הרסנית, חסר-credential, consent בנקאי, מגבלת spend).

## 2. מצב נוכחי מאומת (2026-07-04, HEAD `1a15362`)

- **566 טסטים עוברים**, `qa_gate.py` ירוק (6/6 סעיפים אוטומטיים; סעיף 7
  ידני-בלבד), 16/16 `prod_smoke.py` מול `https://cfo-2.vercel.app`.
- גל 1+2 מה-handoff המקורי (`2026-07-03-epic1-handoff.md`) הושלמו ונפרסו.
- 13 איטרציות המשך בוצעו (ר' `.superpowers/sdd/progress.md`), כולן עם
  TDD מלא + פריסה + אימות חי בפרוד לכל שינוי-קוד. תמצית:
  - תוקן: hook אוטומטי שיצר שורות-זבל בטבלאות Account/Transaction בכל
    sync אמיתי (השורש: השוואת קוד-מסמך מספרי מול string).
  - תוקן: `AdvancedAIService` (`/ai-analytics`) ייצר תחזיות מ-`random.*`
    והזין הקשר פיננסי קשיח זהה-לכל-ארגון ל-GPT; עכשיו מחזיר 400 כנה.
  - תוקן: `alert_engine` + `cfo_brain_service` — כיסוי-טסטים מלא נוסף
    (17+32 טסטים), נמצא ותוקן באג אזור-זמן שקט + חוסר-בידוד בין מחוללי-
    תובנות (מחולל אחד שנכשל היה מבטל את כולם, בשקט).
  - תוקן: `/analytics` (AnalyticsDashboard.tsx) היה 100% mock ללא שום
    קריאת API — הוסר לגמרי, מנותב ל-`/kpis` האמיתי.
  - נבנה: "קישור תשלום" על חשבוניות AR (SUMIT `beginredirect`) — אומת
    חי (הגיע נכון ל-SUMIT, קיבל שגיאה עסקית אמיתית — ר' סעיף 3.6).
  - תוקנו כמה טענות מיושנות ב-`docs/PRODUCT_AUDIT_AND_ROADMAP.md` (VAT
    split, ראוטים, AR hardcodes, form 102/126) — כבר היו מתוקנות בקוד
    ולא שיקפו זאת בתיעוד.
  - נחקרו ונדחו בכוונה (לא מומשו): טריגרי-SUMIT (webhook, CRM-בלבד לא
    שימושי ל-real-time sync אמיתי), PCN874 (spec לא נמצא), מבנה אחיד
    (spec נמצא ואומת, אך feature גדול מדי לסבב אחד), ריבית חוק מוסר
    תשלומים (פער בשיעור שדורש אימות משפטי).

## 3. שאלות פתוחות — לענות/לחקור/לתעד לפני תכנית ההמשך

### 3.1 ANTHROPIC_API_KEY (חוסם, עדיפות עליונה)
**מצב**: המשתמש התחייב להוסיף בעצמו ב-Vercel; נבדק שוב ושוב
(`vercel env ls production | grep -i anthropic`) — **עדיין לא נוסף**
נכון לרגע כתיבת מסמך זה. **פעולה נדרשת**: בדוק שוב עכשיו. אם נוסף:
1. `vercel --prod --yes` (דיפלוי מחדש — משתני-סביבה לא נכנסים לתוקף
   בלי דיפלוי חדש). 2. בדיקה חיה: שאלת מידע-בלבד קודם (למשל "מה מצב
   גיול החובות"), ורק אז פעולת-כתיבה עם אישור (ר' אזהרה בסעיף 3.7 לגבי
   `customer_id`). 3. עדכן `PRODUCTION_READINESS.md` + ledger + דווח
   למשתמש. זה סוגר את גל 2 רשמית.

### 3.2 החלטת repair/retire — מערכת Account/Transaction הישנה
**הממצא המלא** (P0, ר' `docs/PRODUCT_AUDIT_AND_ROADMAP.md` תחת "ממצא
חדש — שתי מערכות חשבונאות מקבילות" ו-".superpowers/sdd/progress.md"
מהאיטרציה השנייה של הסבב הזה): `financial_reports_service.py` (מזין
`/api/reports/*`) ועוד **7 שירותים אמיתיים** (`ai_analytics_service`,
`ai_intelligence_agent`, `balance_snapshot`+`kpi_service` בעקיפין,
`budget_service`, `cost_analysis_service`, `fees_service`,
`forecasting_service`) קוראים מטבלאות `Account`/`Transaction` — נפרדות
לגמרי מה-`Invoice`/`Bill` האמיתיים שמזינים את `/ledger` (התקין,
מאומת). ה-hook האוטומטי שיצר שורות-זבל שם **כבר נעצר** (איטרציה 1),
אבל **127+ שורות זבל קיימות (org1) נשארו קפואות במקום**, ו-8 היכולות
שברשימה **עדיין** בונות על נתון קפוא/חלקי.
**שאלה למשתמש**: repair (לחבר את 8 השירותים ל-Invoice/Bill האמיתיים —
פרויקט גדול, לכל שירות בנפרד) / retire (להחליף את `/reports` ב-הפניה
ל-`/ledger`, ולבדוק כל אחד מ-7 השירותים האחרים אם ניתן להעביר למקור
אמיתי או להצניע) / להשאיר כפי שהוא (אם מישהו סבור שזו כוונה מוצרית
נפרדת — לא נראה סביר לפי הראיות, אך לא ניתן לשלול מהקוד בלבד). **אל
תחליט לבד** — זו החלטת ארכיטקטורה/מוצר.

### 3.3 ניקוי שרידי בדיקה חיה ב-SUMIT
מסמך 1001 (הצעת מחיר, ₪1, לקוח "בדיקת מערכת רצף — למחיקה", מזהה SUMIT
`2095660684`) — ניסיון ביטול נכשל ("Cancelling this document isn't
allowed" — מגבלה עסקית אמיתית של SUMIT להצעות-מחיר, לא באג). אין
endpoint מחיקה בכלל ב-API (אומת מול המפרט המלא). **דורש טיפול ידני
בממשק SUMIT** (app.sumit.co.il) — לא ניתן לביצוע ע"י סוכן (דורש
התחברות אמיתית שאין לו).

### 3.4 מבנה אחיד — spec אמיתי בידיים, לא מומש
`docs/PRODUCT_AUDIT_AND_ROADMAP.md` שורה 114 מכיל URL מאומת של המפרט
הרשמי המלא (רשות המסים, גרסה 1.31/2009, טבלאות שדה מדויקות לכל רשומה:
A100/Z900/C100/D110/D120/B100/B110/M100). **זו לא אותה דרישה כמו
PCN874** (תוקן הבלבול הזה השבוע). זהו פרויקט-מימוש גדול (מיפוי
Invoice/Bill/Payment/Account/InventoryItem שלנו ל-8 סוגי-רשומה) הראוי
לתכנון TDD ייעודי נפרד — לא סעיף בתוך תכנית-המשך כללית. **שאלה**: האם
זו בעדיפות לסבב הבא, או שיש דברים דחופים יותר?

### 3.5 PCN874 (דוח מע"מ תקופתי) — spec עדיין לא אותר
בניגוד ל"מבנה אחיד", לא נמצא מקור קריא-מכונה למפרט המדויק (רק מדריכי-
משתמש של ספקי-תוכנה). ייתכן שדורש פנייה ישירה לרשות המסים / חיפוש
נוסף. **אל תממש ניחוש** — קובץ מס שגוי-אך-משכנע מסוכן יותר מה-pipe-
delimited הנוכחי שברור שאינו רשמי.

### 3.6 ריבית חוק מוסר תשלומים לספקים — פער בשיעור, לא מומש
נמצא פער בין מה שהרוד-מאפ טען ("Prime+2%") למה שמחקר-רשת בסיסי מצא
("Prime+6.5%" לפי "חוק פסיקת ריבית והצמדה" שהחוק מפנה אליו,
https://www.nevo.co.il/law_html/law00/144599.htm). **לא מומש בכוונה**:
זה חישוב שקובע בפועל כמה עסק גובה מלקוח אמיתי — טעות משפטית/כספית
ממשית. **דורש אימות מול נוסח החוק המלא, או עו"ד/רו"ח**, לא רק חיפוש-
רשת, לפני מימוש.

### 3.7 "קישור תשלום" (payment-link, נבנה ונפרס) — חסם-אימות מלא
הפיצ'ר עצמו נבנה, נבדק (10 טסטים), ונפרס. אימות חי מול חשבונית אמיתית
(org1, #13, ₪7,500) הראה שהקריאה מגיעה נכון ל-SUMIT אך נדחית: "מודול
סליקת אשראי אינו מותקן בעסק" — **מגבלה בחשבון ה-SUMIT הספציפי הזה**,
לא באג. כדי לבדוק הצלחה מלאה, צריך חברה עם מודול Upay מופעל (ר' סעיף
"wallet activation" ב-`SUMIT_MODULE_COVERAGE.md`). **אזהרה נוספת**:
הצ'אטבוט (סעיף 3.1) עדיין אין לו כלי בירור-לקוח — אם ישאלו אותו לבדוק
תשלום ללקוח בשם, הוא עלול להעביר שם כ-`customer_id`, מה שכבר יצר בעבר
שרידי-לקוח מזוהים ("2095660683") שנוצרו כך בטעות. שים לב לזה בבדיקה
החיה הראשונה (סעיף 3.1).

### 3.8 פערים קטנים יותר, לא-חוסמים (P1/P2, מתועדים, לא נבדקו לעומק)
- Masav: אין ולידציית פרטי-בנק ספק (אומת עדיין רלוונטי, איטרציה 13).
- שכר: `form_102`/`form_126` מחשבים סיכום אמיתי אך לא קובץ XML רשמי.
- הוצאות: אין זיהוי כפילויות מסמכים; אין מנגנוני-ניכוי (רכב/בית/טלפון).
- שני endpoints כפולים ל-AR aging (שטוח + nested) — שקול לאחד.
- Open Finance: מסך הפעלה + הוראות קיימים כעת (איטרציה 8), אך מסע-
  consent אמיתי מול בנק לא נבדק חי (דורש בנק אמיתי + המשתמש).

## 4. קבצי-רקע חיוניים (קרא אותם, אל תשכפל את התוכן שלהם)

- `.superpowers/sdd/progress.md` — יומן מלא, append-only, מקור-אמת.
- `docs/PRODUCT_AUDIT_AND_ROADMAP.md` — גריד-יכולות + P0/P1/P2, מעודכן.
- `SUMIT_MODULE_COVERAGE.md` — כיסוי מודולי SUMIT, מעודכן.
- `docs/PRODUCTION_READINESS.md` — מצב סביבת פרוד.
- `docs/superpowers/plans/2026-07-03-rezef-goal-directive.md` — המטרה.

## 5. מה מצופה מתכנית ההמשך שתפיק

תכנית ממוקדת (לא כל הפערים בבת אחת) הבוחרת 3–5 הפריטים בעלי הערך
הגבוה ביותר מסעיף 3, לפי סדר-העדיפויות שבהנחיית-המטרה (נכונות-נתונים
› יכולת חוסמת › חוויית-משתמש › צ'אטבוט › ליטוש), עם שלבי TDD קונקרטיים
לכל אחד — בדיוק כמו `2026-07-03-epic1-handoff.md`. לכל שאלה שדורשת
קלט-משתמש (3.1 החלק שכבר תלוי בו, 3.2, 3.6) — סמן אותה כ"ממתין
למשתמש" בתכנית, אל תניח תשובה.
