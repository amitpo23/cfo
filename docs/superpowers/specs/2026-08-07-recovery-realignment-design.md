# אפיון התאוששות ויישור קו — חזרה למערכת שלמה

**2026-08-07 · אושר במועצה (Opus 5; Codex לא זמין — מכסה) · כפוף ל-`docs/MASTER_EXECUTION_PLAN.md`**

## 1. האבחנה — איפה התבלבלנו

| # | שורש | ראיה |
|---|---|---|
| 1 | חסמי-בעלים הפכו לתירוץ לעבודה צדדית | שער 0 תקוע מ-24/07 (obligo org1, בנק org5 — שניהם עדיין פתוחים); בינתיים נבנו מושקו/ערוצים/control-plane שאינם בשער הפעיל |
| 2 | שער ה-deploy הפך לצוואר בקבוק מבני | שבועות בלי deploy; שתי מיגרציות ממתינות; 1,723 טסטים ירוקים לוקאלית מול פרוד מפגר — "עובד בטסטים" ≠ "עובד" |
| 3 | "הלוח היחיד" בלי אכיפה | מאז 24/07 נוצרו שתי תכניות מושקו חדשות; branch אחד ארוך-חיים צבר ~2,200 שורות לא-committed מ-6 נושאים |
| 4 | החוליה השבורה נשארה שבורה | `journal_entries=0` בכל הארגונים; `createbatch` — 0 מופעים ב-`src/` |

תסמין מצטבר: code-review רב-סוכני (07/08) אימת **10 פגמים** ב-diff של ה-branch, ובראשם OCR שדורס סכום מאומת ידנית ומתייק מע"מ שגוי — הפרת honest-null שנולדה בדיוק מהעומס על ענף אחד צפוף.

## 2. ההכרעה

**כיוון א': ייצוב → פריסה → החוליה השבורה.** נדחו:

- **ב' (createbatch קודם)**: מגדיל את מחלת הבנוי-לא-פרוס, ובונה רישום בלתי-הפיך מעל באג נתונים ידוע (VAT=0 במסמכים מסונכרנים).
- **ג' (reset ל-main)**: ה-branch הוא 17 commits קדימה / 3 אחורה בלבד; פירוק ל-cherry-picks יקר מכל מה שהוא מונע ומאבד היסטוריית ביקורת.

הכרעות הבעלים המעגנות: הלולאה החשבונאית קודמת; מושקו מתקדם רק כשהלולאה ירוקה; deploy מסודר מאושר; המסלול הפתוח היחיד — org5 (SUMIT חי, בלי בנק).

## 3. תכנית ההתאוששות — חמישה שלבים

### שלב 1 — תיקון בסיס ה-QA (שער חוסם)
1. rebase על `origin/main` (3 commits).
2. הכנסת `reports/2026-08-05-cross-org-consistency-audit.md` ל-git (negation ב-`.gitignore`) — הראיה שה-registry מצביע עליה חייבת להיות versioned.
3. ריצת pytest מלאה על checkout נקי (`git stash -u` או clone). התוצר: מספר טסטים בר-שחזור על base עדכני.

### שלב 2 — תיקון 10 הממצאים
סדר: **פגם ה-OCR ראשון ובנפרד** + טסט רגרסיה "סכום מאומת (≠0) לעולם לא נדרס ע"י extraction". אחריו התשעה הנותרים (וואטסאפ template רב-שורות, cutoff חוצה-ספקים, audit rollback, בליעת חריגות כלי + session מורעל, ברירת מחדל של provider, סטטוס skip, פילטר NULL, audit ריק ביבוא, הראיה — נסגרה בשלב 1). TDD לכל תיקון.

### שלב 3 — סגירת ה-branch: 3 PRs לפי גרעיניות rollback
- **PR1**: עשרת התיקונים + הראיה. אפס שינוי סכימה; revert בלי לגעת ב-DB.
- **PR2**: שתי המיגרציות (`d6e7f8a9b0c1`→`e7f8a9b0c1d2`, head יחיד) + הקוד התלוי בהן. ה-PR היחיד עם צעד פרוד לא-הפיך — שער נפרד.
- **PR3**: היתרה (roster-coverage, intake-preflight, יבוא אינדקס, control plane).

לא מפרקים רטרואקטיבית ל-PRs נושאיים — ההיסטוריה שזורה והרווח קוסמטי.

### שלב 4 — deploy מסודר (אישור בעלים)
לפי `GATE0_DEPLOYMENT_RUNBOOK.md`: מיגרציות → smoke → מחזור בוקר רץ למחרת עד 06:45 על הנתונים הקיימים.
**מנגנון "מושקו נפרס אך לא מופעל"** (מפורש, לא כוונה): סודות הערוצים (`telegram_bot_token`, `whatsapp_access_token`+`phone_number_id`) **לא מוגדרים בפרוד** — כל push מסתיים `not_configured`/noop (מאומת ב-`channel_notifier.py:91-93`). הפעלת ערוץ = החלטת בעלים נפרדת אחרי שהלולאה ירוקה.

### שלב 5 — שער 3: החוליה השבורה על org5
- **קדם-תנאי חוסם**: אימות שהמסמכים המסונכרנים של org5 נושאים פיצול מע"מ אמיתי (לא VAT=0). אם לא — מתקנים את הסנכרון קודם.
- עטיפת `books/transactions/createbatch` → dry-run על מנה חדשה ב-org5 → השוואה שורה-שורה מול הפורטל → צינור תיוק→פקודה → סגירת מנה באישור בעלים בלבד (לפי לוח שערים 3.1–3.6 בתכנית הראשית).

## 4. שלושה מנגנוני משמעת (שהסחף לא יחזור)

1. **אכיפת החניון**: כל סשן נפתח בשלוש שורות מהתכנית הראשית §11 — איפה נעצרה הלולאה, מה השער הפעיל, עדכון הלוח בסוף. עבודה שאינה בשער הפעיל נרשמת בחניון ולא מתבצעת — כולל רעיונות מושקו.
2. **קצב פריסה**: PR שנטמע ל-main נפרס תוך ≤3 ימי עבודה (באישור בעלים) או שנרשמת סיבה בלוח. אין עוד "בנוי, לא פרוס" כברירת מחדל.
3. **משמעת ענפים**: branch חי ≤שבוע ונושא נושא אחד. ה-branch הנוכחי הוא האחרון מסוגו.

## 5. גורל מושקו

קופא בחניון עד ששער 4 ירוק (שבוע בריפים מלאים על הפיילוט). ההשקעה לא נזרקת — הקוד נפרס כבוי וממתין. הפעלה = החלטת בעלים + סודות + תבנית Meta מאושרת.

## 6. הגדרת הצלחה

- **קצר** (שלבים 1–4): פרוד = main; מחזור בוקר חי; אפס ממצאי review פתוחים; roster-health מתריע.
- **אמצע** (שלב 5): שבוע שבו כל מסמך שנקלט ב-org5 הגיע לספרים בלי הקלדה.
- **הצפון** (ללא שינוי): חודש קלנדרי ירוק מלא על תיק הפיילוט — ואז סקייל.

## 7. סטטוס החלטות בעלים

| החלטה | סטטוס |
|---|---|
| כיוון א' + 3 PRs | הוכרע במועצה בהסמכת בעלים ("תתייעץ ותחליט") |
| אישור deploy בפועל (שלב 4) | נדרש אישור מפורש ברגע האמת — לא מראש |
| obligo org1 / בנק org5 | פתוחים; אינם חוסמים שלבים 1–5 |
| הפעלת ערוצי מושקו | קפוא עד שער 4 ירוק |
