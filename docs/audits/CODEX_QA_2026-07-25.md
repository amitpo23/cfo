# סבב בדיקות Codex — 2026-07-25

ענף: `chore/repo-order-and-control-plane` · HEAD: `0087c63` · Python: `3.12` (`.venv/bin/python`; הפקודה `python` אינה ב־PATH)

## 1. בסיס

| שער | תוצאה | דלתא מהבסיס |
|---|---|---|
| pytest | **PASS** — 1,324 עוברים / 0 נכשלים / 0 דילוגים · 242.18 שניות · 18,728 אזהרות | עוברים: 0; נכשלים: 0; אזהרות: ‎+32 לעומת 18,696 שב־`AGENTS.md` |
| frontend | **PASS** — `npm ci && npm run build` (`tsc` + Vite, ‏2,429 מודולים) | ללא שינוי; `npm run lint` שבור מראש ולא נבדק |
| audit_routes | **BASELINE PASS** — 248 routes: ‏172 תקין, ‏39 אזהרה, ‏36 מוגדר־סביבה, כשל מסווג 1 | דלתא מ־1: 0 |
| schema_drift | **BASELINE FAIL** — אותן 4 טבלאות ואותן עמודות חסרות ב־SQLite המקומי | אין תוספת לרשימה הידועה |
| qa_gate | **BASELINE FAIL** — 6 PASS, ‏1 FAIL (`3a. Schema drift (local)`), ‏1 SKIP (`3b. Schema drift (Neon)`) | אין שער אדום נוסף |

הכשל היחיד ב־`audit_routes` הוא `/api/financial/ai/predict/revenue` שמחזיר 400 כי אין היסטוריית נתונים אמיתית. זו התנהגות honest-null צפויה, לא רגרסיה.

`schema_drift_check` החזיר בדיוק את הבסיס: `filing_crosschecks`, `morning_briefs`, `of_snapshot_cache`, `vehicle_profiles`, והעמודות הידועות ב־`organizations`, `accounts`, `daily_snapshots`, `expenses`.

## 2. ממצאים

### P0 — פעולות כספיות חיות פתוחות לכל משתמש פעיל, ללא הרשאת בעלים

**ראיה:** `src/cfo/api/__init__.py:138-141` מחבר את נתיבי SUMIT עם `get_current_user` בלבד; `src/cfo/api/dependencies.py:80-92` מחזיר כל משתמש פעיל ללא בדיקת תפקיד. לאחר מכן `src/cfo/api/routes/payments.py:27-35` מחייב לקוח, `src/cfo/api/routes/payments.py:143-151` מבטל חיוב חוזר, ו־`src/cfo/api/routes/payments.py:168-176` יוצר עסקת אשראי. אותו דפוס קיים ב־Open Finance ב־`src/cfo/api/routes/open_finance.py:647-692`.

**תרחיש כישלון:** משתמש ארגוני בתפקיד `viewer` שולח `POST /api/payments/charge` עם `ChargeRequest` תקין → `get_current_user` מאשר אותו וה־route קורא ישירות ל־SUMIT, אף שאין אישור בעלים או אפילו הרשאת admin.

**תיקון מוצע:** לחייב תפקיד בעלים/מנהל מאושר ושער אישור מפורש, מתועד וחד־פעמי בכל route של חיוב, ביטול, החזר, mandate או שידור מסמך.

### P0 — `FinancialService` אינו מבודד לפי ארגון

**ראיה:** `src/cfo/services/financial_service.py:20-42` אינו מקבל `organization_id`; `get_account` מסנן רק לפי PK ו־`get_all_accounts` מחזיר את כל הטבלה. `src/cfo/services/financial_service.py:44-69` גם מאפשר לעסקה לעדכן את החשבון שהוחזר כך. שאילתות העסקאות והסיכום ב־`src/cfo/services/financial_service.py:72-125` אינן מסוננות לפי ארגון.

**הוכחה offline:** במסד זיכרון עם `A-bank` בארגון 1 ו־`B-bank` בארגון 2, `FinancialService(s).get_all_accounts()` החזיר `[(1, A-bank, 100), (2, B-bank, 200)]`.

**תרחיש כישלון:** צרכן של השירות פועל בשם ארגון 1 וקורא `get_all_accounts()` או `get_financial_summary()` → הפלט כולל גם את נתוני ארגון 2; `create_transaction(account_id_of_org_2, ...)` אף עשוי לשנות את יתרת הארגון האחר. לא נמצא route פעיל שמבנה כיום את השירות, אך המחלקה מיוצאת מ־`src/cfo/services/__init__.py` ולכן זה פגם שירותי קיים ולא הגנה.

**תיקון מוצע:** להפוך `organization_id` לפרמטר חובה בבנאי ולסנן באמצעותו כל read/write, כולל אימות שהחשבון והעסקה שייכים לאותו ארגון.

### P1 — יצואי רשות המסים ניתנים להורדה בלי שער אימות, ומאבדים את סימון הטיוטה

**ראיה:** `src/cfo/services/pcn874.py:9-20` מצהיר שהמבנה אינו מאומת, ו־`src/cfo/services/pcn874.py:63-66` משתמש כברירת מחדל בח״פ `000000000`. למרות זאת `src/cfo/api/routes/daily_reports.py:182-205` מחזיר קובץ PCN874 להורדה בלי לחייב `filing_verification` ובלי להעביר את `draft`/`disclaimer`. ב־OPENFRMT קיימים מיפויים משוערים ושדות אפס placeholder ב־`src/cfo/services/openfrmt.py:7-13` וב־`src/cfo/services/openfrmt.py:127-137`, אך `src/cfo/api/routes/daily_reports.py:209-246` מחזיר ZIP בשם `OPENFRMT` ומעביר רק header של encoding.

**הוכחה offline:** לארגון עם `tax_id=514999999`, קריאה ל־`build_pcn874` בלי הפרמטר האופציונלי יצרה `PCN874_000000000_202606.txt` ושורת פתיחה `O000000000`; ‏`build_openfrmt` החזיר `draft=True`, רשימת placeholders ומיפוי מסמך שמוגדר במפורש “גס, לא מאומת”.

**תרחיש כישלון:** בעל עסק מוריד `/api/daily-reports/pcn874/file?year=2026&month=6` בלי `company_vat_id` → מקבל HTTP 200 וקובץ דיווח עם ח״פ אפסים, ללא אזהרת טיוטה וללא שלוש בדיקות עצמאיות. אותו משתמש מוריד OPENFRMT → מקבל ZIP בר־ייבוא שמכיל קודים ושדות placeholder לא מאומתים, בעוד ה־disclaimer נשאר באובייקט פנימי שנזרק.

**תיקון מוצע:** לחסום הורדת קובץ רגולטורי עד `verify_filing` שעבר שלוש בדיקות ועד אימות המבנה הרשמי; לקחת ח״פ מהארגון, לדחות ח״פ חסר, ולצרף סימון טיוטה גלוי כל עוד המבנה לא מאומת.

### P1 — מסלולי sync ידניים עוקפים את שער העלות של 20 שעות

**ראיה:** השער הקשיח נמצא ב־`src/cfo/api/routes/cron.py:126-156` ומופעל במסלולי cron. לעומתו `src/cfo/api/routes/office.py:142-179` מפעיל `_run_sync` עבור לקוח יחיד או כל לקוחות המשרד, ו־`src/cfo/api/routes/office.py:215-225` קורא ישירות ל־`SyncEngine.run_full_sync`. גם מסלול super-admin ב־`src/cfo/api/routes/admin.py:877-908` קורא ישירות למנוע. בתוך `src/cfo/services/sync_engine.py:188-241` יש lock נגד חפיפה, אך אין בדיקת interval לפני תחילת הקריאות.

**תרחיש כישלון:** משתמש משרד שולח `POST /api/office/sync-all`, ממתין לסיום ושולח שוב בתוך שעה → ה־lock כבר שוחרר, `_daily_budget_gate` אינו נקרא, ונפתחת ריצת SUMIT/Open Finance מלאה נוספת לכל לקוח למרות checkpoint צעיר מ־20 שעות.

**תיקון מוצע:** לרכז את שער העלות בתוך `SyncEngine` או wrapper יחיד שכל caller חייב לעבור בו; override ידני צריך להיות בעלים בלבד, מפורש, מתועד ומוגבל.

### P1 — API “ביצוע תשלום” מחזיר הצלחה מומצאת עבור חשבון שאינו קיים

**ראיה:** `src/cfo/services/payment_orchestration.py:14-21` אינו קורא DB כלל: הוא מחזיר הצעות ריקות, reference סינתטי ו־`pending_execution`, וסטטוס עם `vendor="Unknown"` ואפסים. `src/cfo/api/routes/phase10_12.py:46-63` עוטף את התוצאה כ־`{"status": "success"}`. הטסט `tests/test_phases_10_12.py:40-54` מקבע את הפלט הסינתטי במקום לבדוק קיום חשבון, persistence או connector.

**הוכחה offline:** בארגון ללא חשבונות ספק, `execute_payment(999999, "bank_transfer")` החזיר `status=pending_execution`, ‏`amount=0.0` ו־`PAY-999999-BANK_TRANSFER`; `get_payment_status(999999)` החזיר `draft` ואפסים במקום not-found.

**תרחיש כישלון:** לקוח שולח `POST /api/advanced/payments/execute` עם `bill_id=999999` → מקבל HTTP 200, “success” ו־reference שנראה אמיתי, אף שלא קיים Bill ולא נשמרה או בוצעה פעולה.

**תיקון מוצע:** להסיר/לחסום את הנתיב עד למימוש אמיתי, או לאמת Bill מסונן־ארגון, לשמור intent/attempt ולדווח `not_configured`/404 במקום הצלחה סינתטית.

### P1 — תחזית ללא יתרת פתיחה מציגה אפס אמיתי לכאורה וביטחון 95%

**ראיה:** `src/cfo/api/routes/phase10_12.py:90-102` מאפשר `starting_balance=None`; `src/cfo/services/forecasting_advanced.py:23-43` ממיר אותו ל־0. מנוע התחזית מסמן כל תקופה ב־`confidence: 0.95` ב־`src/cfo/services/forecasting_service.py:162-195`, גם כשהיסטוריית ההכנסות וההוצאות ריקה. הטסט `tests/test_phases_10_12.py:150-155` קורא ל־route בלי יתרת פתיחה ומוודא רק שקיים מפתח `forecast`.

**הוכחה offline:** ארגון ללא חשבונות וללא תנועות החזיר `starting_balance=0`, ‏`ending_balance=0` ותקופה עם כל הערכים 0 ו־`confidence=0.95`.

**תרחיש כישלון:** ארגון שעדיין לא סנכרן בנק קורא `GET /api/advanced/forecast/cash-flow?days_ahead=30` → מקבל תחזית “success” של יתרה אפס וביטחון 95%, במקום “לא ידוע/אין נתונים”; מנהל עלול לפרש אפס כיתרה אמיתית.

**תיקון מוצע:** לגזור יתרת פתיחה ממקור בנקאי טרי עם provenance, או להחזיר honest-null/400 כשאין יתרה והיסטוריה מספקת; confidence חייב לשקף כמות נתונים.

### P2 — הטסטים משמרים את התוצאות השגויות ואינם מגנים על הנתיבים הקריטיים

**ראיה:** `tests/test_phases_10_12.py:25-70` בודק shape ומאשר במפורש `pending_execution`; ‏`tests/test_pcn874.py:127-139` בודק רק שהקובץ ניתן להורדה; ‏`tests/test_openfrmt.py:76-92` בודק ZIP ושמות קבצים אך לא disclaimer/שער אימות; ‏`tests/test_sync_call_protection.py:546-568` בודק את helper של שער העלות אך אין בדיקה שמסלולי office/admin משתמשים בו. לא נמצאה בדיקת viewer נגד פעולות חיוב/ביטול.

**תרחיש כישלון:** כל ששת המצבים לעיל קיימים, אך `python -m pytest tests/ -q` עדיין מחזיר 1,324/0, ולכן שער ה־CI ירוק ואינו מזהה דליפת ארגון, פעולה כספית ללא הרשאה, קובץ מס שגוי או עקיפת מכסה.

**תיקון מוצע:** להוסיף לאחר תיקון הקוד טסטי negative/authorization, org-isolation, filing-gate ו־cost-gate שמאמתים פלט ו־side effects ולא רק response shape.

### צירים שנבדקו ללא ממצא מאומת

- לא נמצאה אף הופעה של `current_user.get(...)` ב־`src/cfo/` או בטסטים.
- לא נמצאה קריאת `date_trunc` פעילה; `forecasting_service.py` מבצע אגרגציה חודשית ב־Python במפורש לצורך תאימות SQLite/Postgres.
- נסרקו שימושי `utcnow()` מול `datetime.now(timezone.utc)`; לא אומת תרחיש Python פעיל נוסף של השוואת naive ל־aware מעבר לאזהרות ה־deprecation הידועות, ולכן לא נרשם ממצא.
- נסרקו יחסים וחילוקים במסלולים הפיננסיים; לא נרשם ממצא NaN נוסף בלי consumer פעיל ותרחיש כשל מוכח.

## 3. לא נבדק

- **לא נבדקו SUMIT, Open Finance או `cfo-2.vercel.app` בפועל** — נאסר במפורש כדי למנוע עלות ופעולה חיה.
- **לא נבדקה סכימת Neon/פרוד** — `qa_gate` רץ ללא `--env-file`; שער `3b` דולג כדין.
- **לא הופעל בודק הקבצים הרשמי של רשות המסים ולא אומת מפרט PCN874/OPENFRMT עדכני** — הקוד עצמו מגדיר את המבנים כטיוטה; בדיקה חיצונית לא הייתה בסביבת האודיט.
- **לא הורץ `npm run lint`** — פער ידוע: אין קונפיג ESLint. `npm ci` דיווח 2 חולשות moderate ו־4 high; exploitability ונתיב כשל לא נבדקו ולכן אינן מוצגות כממצא.
- **לא בוצעו commit, push, deploy או שינוי קוד.**

## 4. הכרעה

**חסום.** בסיס הרגרסיה יציב: 1,324 טסטים עוברים, frontend נבנה, route audit תואם, ואין drift מקומי חדש. עם זאת אין להכריז “ירוק לקומיט/פרוד” כל עוד קיימים:

1. P0 — פעולות כספיות חיות ללא הרשאת בעלים.
2. P0 — שירות פיננסי ללא בידוד ארגוני.
3. P1 — קובצי דיווח טיוטתיים ללא שער אימות משולש ועם ח״פ אפסים כברירת מחדל.
4. P1 — עקיפת שער 20 השעות במסלולי sync ידניים.
5. P1 — הצלחת תשלום ותחזית אפס סינתטיות שמפרות honest-null.
