# אודיט: עומק תור התיוק — תיק עמית פורת (databaseid=2114194229)

**תאריך:** 13/07/2026
**סוכן:** Fable (סוכן אימות, קריאה בלבד)
**שיטה:** דפדפן בלבד (Chrome MCP). אפס קריאות SUMIT API.

## ממצא ראשי

**262 קבצים ממתינים לתיוק** — לא "200+" כפי שמציג מסך התיוק.

מסך התיוק (`/books/fileexpenses/`) מציג "תיוק קבצים (200+ קבצים ממתינים)" — המונה קטום
ב-200 בהתאם לחוק 8 ב-`docs/SUMIT_KNOWLEDGE_BASE.md` (מסך התיוק טוען מקסימום 200 קבצים).

### איך נספר בפועל

1. מתוך מסך התיוק אותר קישור לתצוגת גריד **"כל הקבצים לתיוק"**:
   `https://app.sumit.co.il/f2114194212/v839662703/`
   עם פילטר מובנה: `סטטוס שווה ל-ממתין לתיוק`.
2. הגריד טוען 50 שורות בכל פעם (infinite scroll; נדרש dispatch של אירועי scroll —
   `window.scrollTo` לבד לא מפעיל את הטעינה).
3. גלילה עד מיצוי: הספירה התייצבה על **262 שורות** ונשארה יציבה לאורך 5 בדיקות רצופות
   + גלילת אימות נוספת. אין spinner/סנטינל טעינה פעיל.

### פירוט הראיות (מתוך ה-DOM)

```json
{"totalRows": 262, "statuses": {"ממתין לתיוק": 262},
 "byMonth": {"07/2026": 262},
 "newest": "13/07/2026 07:32", "oldest": "05/07/2026 13:48"}
```

פילוח לפי תאריך הוספה:

| תאריך הוספה | קבצים |
|---|---|
| 05/07/2026 | 259 |
| 10/07/2026 | 1 |
| 12/07/2026 | 1 |
| 13/07/2026 | 1 |

כלומר: כמעט כל התור (259/262) הוא ההעלאה המרוכזת של 05/07/2026, ועוד 3 קבצים
בודדים שנוספו מאז (10/07, 12/07, 13/07 — האחרון: `Invoice-E9T9W969-0034.pdf`).

## הצלבה: דיווח מע"מ מאי-יוני 2026 (אותו תיק)

נקרא במסך `books/vat` לאחר החלפת התקופה למאי-יוני 2026 (טכנית: בחירת "מאי-יוני"
ב-selectbox + submit של טופס ה-DateRange — תצוגה בלבד, לא בוצעה "שמירת הדיווח"):

```
דיווח מע"מ - מאי-יוני 2026
עסקאות חייבות: 0 ₪
עסקאות פטורות: 0 ₪
מע"מ עסקאות: 0 ₪
מע"מ תשומות: 18,136 ₪
מע"מ תשומות על נכסים: 0 ₪
סה"כ לתשלום: -18,136 ₪
```

**ללא שינוי מאז 12/07** (אז נרשם ₪18,136 כולל backlog). עקבי — 262 הקבצים בתור
עדיין לא תויקו ולכן לא הזיזו את דוח התשומות.

## הערות שיטה / חסמים שנתגלו

- ה-selectbox של SUMIT (teva-selectbox) נפתח על אירוע **focus** (לא click); בחירת
  אופציה מעדכנת hidden inputs (`DateRange.Date_From=01/05/2026`,
  `DateRange.Date_To=30/06/2026`) אך הדוח מתרענן רק ב-POST של הטופס ל-`/books/vat/`.
- צילומי מסך (CDP screenshot) קרסו ב-timeout על עמוד המע"מ (עמוד כבד); כל האימות
  בוצע דרך קריאת DOM/טקסט.
- לא בוצעה שום פעולת כתיבה: לא תיוק, לא שמירה, לא שינוי נתונים ב-SUMIT.
